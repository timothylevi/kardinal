<ul class="causes left">
  <h2>Causes</h2>
  <% causes.each do |cause| %>
  <li><%= link_to cause.name.humanize.titleize, cause_url(cause), title: "See all petitions for #{cause.name.humanize.titleize}" %></li>
  <% end %>
</ul>